# Upstream provenance

- Upstream repository: `https://github.com/roukaour/ascii85`
- Upstream default branch: `master`
- Snapshot used: `069f585cbe7642a3f0f08de71ced639ce48375ba`
- Upstream commit title: `Merge pull request #1 from orivej/fix-buffer-overrun`
- Upstream license: MIT

This working repository was reconstructed from the exact current upstream file contents through the GitHub connector because direct network `git clone` is unavailable in the execution environment.

The local Git remote is still recorded as:

```text
upstream https://github.com/roukaour/ascii85.git
```

The first local commit records the upstream snapshot, and the `a85x` branch contains the derivative changes.
